server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/natgeo
    username: root
    password: hbstudent
  #jpa:
   # generate-ddl: true
    #show-sql: true
    #hibernate:
     # ddl-auto: create

   #email configuration for application.properties
  mail:
    host: smtp.gmail.com
    port: 587
    username: tech.blog.smtp@gmail.com
    password: techblog505
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
